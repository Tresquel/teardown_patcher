use std::{io::Error, vec};

use crate::{config::{self, Mod}, teardown};

pub fn patch() -> Result<bool, Error> {
    
    Ok(true)
}

fn list_mods() -> Result<Vec<Mod>, Error> {
    let mods: Vec<Mod> = Vec::new();

    
}